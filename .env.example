# Supabase (publishable values — safe for the client bundle).
# Defaults are baked into src/lib/config.ts; set these to override.
NEXT_PUBLIC_SUPABASE_URL=https://embgaxvyeekbcazcxnwx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
